# Strategy Studio 2.1 ローカル確認

2026-09-25。対象は `http://localhost:8791` の匿名検証用D1のみ。本番・公式登録・GitHubへの書込みは実施していない。

## 自動検証

- `npm test`：7ファイル・102件成功。報酬の既存ゴールデンケース、配置制約、API、ローカルD1の保存・復元・分離を含む。
- 新規8件：配下付き移動、紹介者と入力の不変性、上限と循環、暫定繰上げ、予定サブ、成長プロフィール固定、予約した配置先、タイトル基準到達、期間継続、概数比較など。
- `npm run build`：型チェック・Worker・SPA・PWAビルド成功。
- `git diff --check`：成功。
- 既存2,000 ID・120か月ベンチマーク：約4.9秒（ローカルNode、2候補）。本番Workers CPU時間の保証ではない。

## 内蔵ブラウザでの確認

- 組織図と到達時／計算終了時の月額が主画面に表示される。
- 専用ドラッグハンドルで既存チームを別のアップへ移動すると、図が更新され再計算される。
- 390px幅で選択→配置先指定→移動が成立。ページ全体の横はみ出しなし。
- 元に戻すと元の親へ戻る。比較基準Aは固定される。
- 同じくらいの人数（500 IDを目安）と同じ経過時間（60か月）へ切替し、実際の人数・月額・期間差を確認。
- 20人一括はリーダー1名＋同行19名のテンプレート。同行人数を23へ手入力し、入力欄を離れた時に確定・再計算できる。
- 新しい保存版が作成され、保存リストから開いた結果・配置が復元される。
- 閲覧中のJavaScriptエラーなし。

## 残る公開前確認

- 本番データ・本番Access・本番D1マイグレーション・実URLの確認は未実施。
- 自動ブラウザスクリプトは新UIに更新し構文チェック済み。今回のUI検証は内蔵ブラウザで行い、そのスクリプト自体の実行成功とは扱わない。
- 探索全体の最適解を保証しない。手動モードは指定配置を比較する。人数比較は目安であり、月次の到達実数を表示する。
